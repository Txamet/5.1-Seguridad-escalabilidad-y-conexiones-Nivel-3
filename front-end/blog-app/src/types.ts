export interface UserType {
    id: string;
    name: string;
    email: string
}

export interface PostType {
    id: string;
    title: string;
    content: string;
    author: string;
    createdAt: string;
    updatedAt: string;
    likes: string;
}